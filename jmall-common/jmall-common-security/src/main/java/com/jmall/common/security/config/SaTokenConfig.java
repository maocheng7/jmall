package com.jmall.common.security.config;

import org.springframework.context.annotation.Configuration;

/**
 * Sa-Token 基础配置
 * <p>
 * Token 名称、有效期等参数在 application.yml 中配置。
 * 此类预留用于后续扩展自定义 StpInterface 权限实现。
 * </p>
 *
 * @author jmall
 */
@Configuration
public class SaTokenConfig {
}
